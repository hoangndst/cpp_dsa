#include <bits/stdc++.h>
using namespace std;

int poisonousPlans(vector<int> p) {
    int n = p.size();
    
}

int main() {

    int n; cin >> n;
    vector<int> p(n);
    for(int i = 0; i < n; i++) cin >> p[i];
    vector<int> days(n) = {0};
    stack<int> s;


    return 0;
}